{
  "name": "aftersales",
  "version": "1.0.0",
  "private": true,
  "description": "登记管理系统 Cloudflare Worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:schema": "wrangler d1 execute aftersales --file=./schema/schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
